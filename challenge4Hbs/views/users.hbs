<main>
  <h1>No User</h1>
  {{#if hasUsers}}
    {{#each users}}
      <ul>
        <li>
          {{ this.name}}
        </li>
      </ul>
    {{/each}}
  {{else}}
    <h1>No user found</h1>
  {{/if}}
</main>
